namespace BeatSync.Utilities
{
    /// <summary>
    /// Use to make sure multiple things aren't writing to the same file.
    /// </summary>
    public class FileLock
    {
        // Use named Mutex instead?
    }
}
